import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.molinarimobilerefinish.controloperativo',
  appName: 'Molinari Control Operativo',
  webDir: 'www'
};

export default config;
